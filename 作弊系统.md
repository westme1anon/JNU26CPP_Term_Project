你现在是我的 C++ RPG 项目的主程序员。请读取并分析我们当前项目的代码库，帮我设计并实现一套“基于 JSON 配置的作弊码系统”。

### 【目标与需求】
1. **统一管理**：所有的作弊码、参数类型、对应说明以及修改的变量/调用的函数，必须统一在 `cheats.json` 中配置。
2. **自动解析与注册**：游戏启动时读取 `cheats.json`，根据配置自动注册对应的作弊指令。
3. **反射/映射机制**：设计一个通用的作弊码管理器（CheatManager），能根据 JSON 中的配置自动绑定并修改玩家或游戏状态的数据（如 HP、金币、等级、无敌状态等）。
4. **控制台/指令输入接口**：提供一个 `ExecuteCommand(const std::string& input)` 接口，当用户输入指令（如 "add_gold 1000"）时解析并执行。

### 【请按以下步骤执行】

#### 步骤 1：分析项目结构
请先扫描我们项目中的玩家类或核心数据结构（例如 `Player.h` / `GameState.h` 等），找出目前包含的关键游戏数值（如生命值、金币、经验值、状态标记等）。

#### 步骤 2：生成 `cheats.json` 配置文件
请在项目中创建或生成一个 `cheats.json` 文件示例，包含常用 RPG 作弊码。格式设计建议如下（你可以根据项目实际调整）：
```json
{
  "cheats": [
    {
      "code": "god",
      "type": "toggle_bool",
      "target": "player.isInvincible",
      "description": "切换无敌状态"
    },
    {
      "code": "add_gold",
      "type": "modify_int",
      "target": "player.gold",
      "description": "增加金币数量"
    },
    {
      "code": "full_hp",
      "type": "call_func",
      "target": "Player::RestoreFullHP",
      "description": "满血复原"
    }
  ]
}